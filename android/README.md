# AI Agent Studio — Android companion

Lightweight companion for the Windows CustomTkinter studio.

- Update check via GitHub Releases API
- Fallback: [`latest-release.json`](latest-release.json) on rate limit / network issues
- Links to PC ZIP releases and docs

Build: `./build-apk.sh`
