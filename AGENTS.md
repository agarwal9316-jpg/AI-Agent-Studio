# Project rules for AI Agent Studio

Add instructions Grok CLI should follow in this project.

## Examples
- Prefer editing source under `app/`; start the app with `Launch.bat`.
- Keep org-chart and chat data under `data/` (do not commit secrets).
- When changing the GUI, update `docs/CHANGELOG.md` and version if user-facing.

