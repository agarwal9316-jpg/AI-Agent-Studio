# Assets

- `tray_icon.png` — legacy tray fallback (also refreshed by branding generator).
- `branding/` — **optional branding pack** (app icons, `.ico`, SVG). See [`docs/BRANDING.md`](../docs/BRANDING.md).
